{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "main": "funcs.js",
  "scripts": {
    "build": "browserify js/*.js -t babelify --debug -o bundle.js",
    "watch": "watchify js/*.js -t babelify --debug -v -o bundle.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "elasticsearch": "^13.0.0",
    "jquery": "^3.2.1"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-polyfill": "^6.23.0",
    "babel-preset-env": "^1.4.0",
    "babelify": "^7.3.0",
    "browserify": "^14.3.0"
  },
  "babel": {
    "presets": [
      "env"
    ]
  }
}
